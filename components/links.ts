/* One conversion, one label. Every contact CTA reads "Start a project" and points
   at mailto until a /contact route exists. Subjects carry no em or en dashes. */
export const EMAIL = 'contact@napkindesign.co';
export const MAILTO = `mailto:${EMAIL}`;
export const MAILTO_NEW_PROJECT = `mailto:${EMAIL}?subject=New%20project%20from%20the%20napkin`;
export const MAILTO_FLAT_QUOTE = `mailto:${EMAIL}?subject=Flat%20quote%20request`;
export const MAILTO_LETS_GO = `mailto:${EMAIL}?subject=Let's%20go%2C%20new%20project`;
